{
  "name": "zod-partial-schema",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "prepublishOnly": "npm run build",
    "test": "vitest --typecheck --reporter=verbose",
    "check-types": "tsc -p tsconfig.build.json --noEmit"
  },
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^7.1.12",
    "vitest": "^4.0.14"
  },
  "peerDependencies": {
    "type-fest": "^5.2.0",
    "typescript": "^5.9.3",
    "zod": "^4.1.12"
  }
}
